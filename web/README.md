# OAuth/OIDC Flow Explorer

Install dependencies and start the development server:

```sh
npm install
npm run dev
```

The Vite development server proxies `/api` requests to the Go API at
`http://localhost:8080`.
