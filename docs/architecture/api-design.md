# API Design

Candidate API areas are authentication, identities, applications, clients,
roles, permissions, policies, sessions, tokens, federation, audit, and
administration.

The API design must define authentication and authorization requirements,
errors, versioning, idempotency, pagination, rate limiting, auditability, and
input validation alongside the domain model.
