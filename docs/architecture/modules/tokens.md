# Tokens

Token management includes access tokens, refresh tokens, API keys, metadata,
signing, expiry, revocation, storage, and key rotation. Credentials, tokens,
signing keys, and private keys are secrets.

